---
- hosts: localhost

  tasks:
  - name: Create service catalog item request
    uri:
      url: "https://192.168.2.46/api/generic_objects/46000000000017"
      method: POST
      user: admin
      password: "{{ password }}"
      validate_certs: no
      headers:
        Content-Type:  application/json
      body_format: json
      body:
        action: delete
